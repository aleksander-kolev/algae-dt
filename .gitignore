# ROS 2 / colcon
build/
install/
log/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Editors / OS
.vscode/
.idea/
*.swp
.DS_Store
*Zone.Identifier

# Runtime artifacts (sync logs are generated; keep samples in docs/ if needed)
*.log
sync_metrics_*.csv
.ros/

# Secrets — real lab-laptop credentials go in CREDENTIALS.txt (added to the zip manually, never committed)
CREDENTIALS.txt
*.mp4

# Local working artifacts (review digests, lineage worktrees)
.review-digest.txt
.reconcile-*.diff
.worktrees/
